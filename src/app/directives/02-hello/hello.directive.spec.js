describe('Hola directive', function () {
  //var scope, compile;

  beforeEach(module('tdd'));

  beforeEach(
    //inject(function ($rootScope, $compile) {
    //  scope = $rootScope.$new();
    //  compile = $compile;
    //})
  );

  it('should display hello to the World in Spanish');


});
